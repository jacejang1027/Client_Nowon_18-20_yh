#include<stdio.h>
#include<stdlib.h>

void Add(int item);
int Remove(int item), RemoveAt(int index);
void Clear();

int* data;
int count;
int capacity;
int* tmp;

int main() {






	return 0;
}

void Add(int item)
{
	if (count = 0) {
		data = (int*)malloc(sizeof(int));
	}
}
